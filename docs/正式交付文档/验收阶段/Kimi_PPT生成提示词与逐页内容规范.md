# Kimi 生成项目验收汇报 PPT：提示词与逐页内容规范

> 用途：将本文件中的“可直接复制提示词”整体发送给 Kimi，要求 Kimi 生成可编辑的 16:9 项目验收汇报 PPT。个人姓名、学号、班级、指导教师等信息请在生成前补充；图片材料由本人后续替换。

## 一、可直接复制给 Kimi 的完整提示词

```text
你是一名具有产品设计、软件工程汇报和信息可视化经验的 PPT 设计师。请为我生成一份可直接用于老师初步验收汇报的 16:9、可编辑项目展示 PPT，项目名称为《AI Manga Translator——漫画多语言智能翻译系统》。

【汇报目标】
这不是空泛的项目宣传，而是一次“能运行、能解释、能展示实现过程”的初步验收汇报。PPT 必须让老师清楚看到：项目解决什么问题、用户如何使用、系统如何处理一张漫画图片、关键技术为什么这样设计、目前已经实现了什么、如何验证、AI 协作中人工做了什么，以及项目还有哪些边界。

【必须遵守的事实边界】
1. 项目是一个本地 Web 应用：React/Vite 前端 + FastAPI 后端 + SQLite 和本地文件存储。
2. 主要功能是漫画图片翻译，重点面向日语/英语到中文；图片输入是当前核心输入形式。
3. 当前实际流水线必须按以下顺序展示：检测文字区域 → OCR 识别 → 擦除/图像修复 → 气泡分组 → 按气泡上下文翻译 → 翻译质量检查 → 中文排版渲染。
4. 检测/OCR 采用可插拔引擎设计：默认路线包含 MIT/CTD 与 MIT48/manga-ocr 混合能力，并保留 PaddleOCR/CV 等回退路径。不要把所有引擎说成同时并行运行。
5. 模型推理必须串行：任务管理器使用单 worker 和流水线锁，批量任务只是编排多个子任务，不能宣称批量并发推理。
6. 翻译服务支持配置后端和回退链；项目已有 DeepSeek、OpenAI、DeepL、Google、MyMemory 等适配能力，但是否可用取决于本地配置和网络环境。
7. 图像修复默认可使用 OpenCV 方案，也可配置 LaMa；不能宣称 LaMa 在当前机器上一定已经启用。
8. 质量检查会识别免费/原文回退、译文过短、日文残留、数字遗漏、术语遗漏、模型解释文字等问题。
9. 前端已经形成任务闭环：上传或粘贴图片/文件夹 → 创建任务 → 查看每张图进度 → 预览结果 → 单张重试 → 单张下载或批量 ZIP 下载。
10. 当前界面支持任务侧边栏、专有名词库、图片预览、文本框编辑、文本框新增/删除、拖拽移动、尺寸调整、字体/字号/颜色/方向预览，以及局部擦除和撤销。
11. AI 润色功能复用已有翻译服务，提供自然、口语、热血、搞笑、正式、自定义等风格；失败时保留原译文。不要说成另一个独立大模型已经部署。
12. 已有真实验证结果：截至 2026-09-03，后端测试 171 项通过；前端生产构建成功。不要编造 OCR 准确率、翻译准确率、平均耗时、加速倍数、GPU 型号或用户数量。
13. 不能把历史翻译记录、EPUB/CBZ/PDF、团队协作、云端 GPU、自动按场景选择引擎等规划内容写成已完成能力。

【设计风格】
采用“瑞士国际主义/Swiss International”风格，关键词：大方、克制、专业、技术感、强网格、信息层级清楚。
- 画面比例：16:9。
- 主色：深墨黑/深海军蓝；强调色只使用一种蓝色或蓝绿色。全篇颜色保持一致。
- 使用无衬线字体；标题粗、正文清晰，避免细小字。
- 使用左对齐、严格网格、明显的页码和章节导航。
- 禁止大面积渐变、发光、厚重阴影、过多圆角卡片、彩虹配色、表情包和无意义装饰。
- 技术流程使用可编辑的线条、节点、编号和箭头；不要把核心流程做成无法修改的截图。
- 重要页面采用“深色封面/深色章节页 + 浅色内容页”交替，形成节奏。
- 每页最多使用 2 个主要内容区，不要把内容压成密密麻麻的小字。
- 每页保留底部安全区，用于页码、项目名和演讲导航。

【内容密度硬性要求】
- 共 15 页，顺序固定，不得合并、删减或改成只有标题的目录式 PPT。
- 每页必须有：页标题、1 句本页结论、3—5 条具体内容、至少 1 个结构化视觉元素（流程图/架构图/表格/对照图/截图占位）。
- 每条正文必须包含具体对象、动作、模块或判断依据，避免“提高效率、赋能行业、效果显著、体验良好”等无证据套话。
- 正文每条建议 16—30 个汉字；单个文本框不超过 5 行；不要出现大段论文式段落。
- 每页正文建议 90—170 个汉字，封面和总结页可少一些，但不得空泛。
- 每页最多 6 个视觉卡片；若使用卡片，卡片标题必须对应一个真实模块、真实步骤或真实证据。
- 技术页必须同时说明“做了什么”和“为什么这样做”，不能只罗列技术名词。
- 所有数字都必须来自下方项目事实；没有数据时写“未测量/待补充”，禁止估算或编造。

【图片占位规则】
- 我会在生成 PPT 后自行导入图片。请不要生成虚构的产品截图、虚构人物、虚构数据图或带文字的 AI 配图。
- 需要图片的地方必须预留带边框的占位框，并使用统一命名：
  [图片占位-页码-素材名称-建议比例]
  下面附一行小字：图片说明：放入什么真实材料、用于证明什么。
- 占位框必须标明建议比例，例如 16:9、4:3、1:1 或 3:2；框内不得只写“此处放图”。
- 真实素材优先使用：项目 UI 截图、原图/译图对照、流程截图、AI 协作记录截图、测试命令结果截图。
- 若没有合适图片，使用可编辑的结构图或表格替代，不要用无关漫画人物图填空。

【演讲备注】
- 每页添加 60—100 字中文演讲备注，说明这一页应该怎么讲，以及要把观众注意力引向哪个证据。
- 备注必须与页面内容一致，不要补充页面上没有依据的性能或准确率结论。
- 第 13 页要给出 AI 协作答辩说法：人工提出约束、检查代码和真实页面、发现问题、验证结果并决定是否接受建议；AI 负责代码分析、方案草拟和局部实现。

【输出要求】
请直接生成 PPT 内容和版式，不要只给设计建议。输出应包含：
1. 15 页完整 PPT；
2. 每页可直接放入文本框的标题、正文、图表标签；
3. 每页图片占位框及图片说明；
4. 每页演讲备注；
5. 最后一页附“素材替换清单”；
6. 所有元素尽量保持可编辑，流程图、表格、图标和占位框不要合并成一张不可编辑大图。

【逐页内容硬性规范】

第 01 页｜封面：项目定位
本页必须包含：项目全名“AI Manga Translator——漫画多语言智能翻译系统”、一句定位“面向漫画图片的 OCR、翻译、擦除修复与中文排版一体化工具”、汇报类型“初步验收汇报”、姓名/学号/班级/指导教师占位。
视觉要求：左侧大标题，右侧放一张真实 UI 或原图/译图组合占位，不要放无关装饰图。
图片占位：[图片占位-01-项目主界面或原译对照-16:9]
图片说明：放入实际项目首页或一组真实漫画原图/中文译图，用于建立项目第一印象。

第 02 页｜为什么要做：问题与价值
本页必须说明三个具体痛点：漫画文字存在竖排、弯曲、密集和复杂背景；普通 OCR 只解决识字，不能解决擦除和排版；逐句翻译容易丢失气泡上下文与术语一致性。
必须给出“原始处理链”和“本项目处理链”对照：人工截图/识字/翻译/修图/排版，对比上传图片后自动完成任务流。
视觉要求：左侧痛点对照，右侧放漫画原图局部占位并用标注指出竖排文字、气泡边界和复杂背景。
图片占位：[图片占位-02-复杂漫画文字局部-3:2]
图片说明：放入真实日文或英文漫画原图局部，标出本项目需要处理的具体难点。

第 03 页｜做什么：需求范围与实现边界
本页必须分成“当前已实现”“依赖环境”“后续规划”三栏。
当前已实现至少写：图片/文件夹输入、日英重点翻译、任务进度、结果预览、单张重试、单张/批量下载、词库管理、文本框编辑、局部擦除撤销。
依赖环境必须写：真实 OCR 模型、LaMa、外部翻译 API 是否可用取决于本地安装、模型权重、密钥和网络；核心 Demo 模式可在较少依赖下运行。
后续规划只能写：历史翻译记录、章节级管理、EPUB/CBZ/PDF、团队协作、云端 GPU等。
视觉要求：用边界表，不得把三类内容混在一起。

第 04 页｜用户怎么用：一次完整任务
必须按真实操作顺序展示：上传/粘贴图片或文件夹 → 选择语言与翻译设置 → 提交任务 → 查看每张图进度 → 查看完成预览 → 编辑或重新翻译 → 下载结果。
每一步至少写出一个可观察结果，例如“任务卡出现”“单张完成后立即可预览”“失败子任务可单独重试”。
视觉要求：横向 7 步任务流，右下角预留 UI 录屏/操作截图位置。
图片占位：[图片占位-04-任务流界面录屏截图-16:9]
图片说明：放入上传、任务进度、结果预览中的 2—3 张真实界面截图。

第 05 页｜系统怎么组成：总体架构
必须展示五层：React/Vite 前端交互层、FastAPI API 层、TranslationTaskManager 任务层、Pipeline 推理层、SQLite/本地文件存储层。
必须标注每层职责：前端负责任务与编辑交互；API 负责校验和状态接口；任务层负责单 worker 调度；流水线负责检测到渲染；存储层保存任务元数据和结果路径。
必须标出“批量编排”和“模型推理串行”的关系。
视觉要求：可编辑分层架构图，使用实线表示数据流，虚线表示配置/回退关系。
图片占位：[图片占位-05-系统架构图或代码结构截图-16:9]
图片说明：优先放实际架构图或后端/前端目录截图，证明模块不是概念拼接。

第 06 页｜核心流程：一张图如何变成译图
必须完整、按顺序展示：detect → OCR → inpaint → bubble grouping → translate → quality assessment → render。
每个节点必须同时写“输入/处理/输出”中的至少一项；重点解释为什么先修复背景，再按气泡分组和整块翻译。
必须突出：原图用于保留边界证据，清理图用于气泡内部连通性判断；翻译单位是气泡整块，不是孤立字符。
视觉要求：主流程占页面 2/3，下方用三条短注释解释“检测、翻译、渲染”的关键判断。
图片占位：[图片占位-06-流水线实测流程图或调试截图-16:9]
图片说明：可放代码流程截图、任务日志截图或自行绘制的真实流程示意。

第 07 页｜识别难点：OCR、方向与引擎回退
必须说明：检测先定位文字区域；MIT48 负责主要识别；manga-ocr 主要用于救回 MIT48 完全没读出的空行，避免用低置信度简单覆盖正确结果；PaddleOCR/CV 是可配置回退路径。
必须展示横排/竖排差异，以及自动语言识别和日英重点翻译的关系。
必须写清楚：识别结果包含文本、置信度、文字框/多边形和方向等信息，后续擦除与排版会使用这些结果。
视觉要求：左侧做“文字框—识别文本—方向”示意，右侧做引擎路由表。
图片占位：[图片占位-07-OCR原图与检测框/识别结果-3:2]
图片说明：放入真实检测框截图或 OCR 调试结果，突出竖排和风格化字体。

第 08 页｜图像重建：擦除、修复与气泡分组
必须写出三个真实机制：掩膜由文字多边形、Otsu 笔画候选和注音扩展合并；默认 OpenCV 局部修复，可选 LaMa；非气泡文字如刊头/拟声词等会被保留，不进入擦除、翻译和渲染。
必须解释“清理图分组 + 原图边界否决”的设计，说明它如何降低跨气泡合并和整页吞并风险。
必须展示几何回退：可靠气泡掩膜 → 安全扩展框 → 紧致文本框 → 跳过该气泡。
视觉要求：使用“原图/掩膜/修复图/分组结果”四格对照，并配一条回退链。
图片占位：[图片占位-08-原图掩膜修复分组四格对照-4:3]
图片说明：放入同一张真实图片的四阶段结果，证明修复不是简单涂白。

第 09 页｜翻译怎么更稳：上下文、回退与质量检查
必须说明：以气泡为单位拼接文本并一次翻译，尽量保留原文换行和句子顺序；术语库参与整词替换，保持专有名词一致；翻译服务按配置后端运行，失败时进入回退链。
必须列出质量检查项：回退后端提示、译文过短、日文残留、数字/单位/符号遗漏、术语遗漏、模型附带解释文字。
必须补充 AI 润色：复用已有 DeepSeek 翻译服务，支持多种风格；失败保留原译文。
视觉要求：左侧做“气泡上下文 → 翻译 → 检查”流程，右侧放质量告警示意表。
图片占位：[图片占位-09-词库/翻译结果/质量告警界面-3:2]
图片说明：放入真实词库界面或翻译结果中质量提示的截图。

第 10 页｜译文如何落回画面：排版与人工编辑
必须说明横排和竖排采用不同排版策略：横排进行均衡换行和字号选择；竖排按气泡分列、按原文顺序分配，并控制列宽和字距。
必须展示渲染安全机制：文字与气泡掩膜覆盖率不足时回退矩形裁剪；气泡几何不可靠时按安全扩展框、紧致框分级处理。
必须写出用户可操作编辑：拖拽文本框、调整尺寸、修改字体/字号/颜色/方向、添加或删除文本框、局部擦除并撤销。
视觉要求：左侧原译对照，右侧用编辑器界面截图占位，底部列出“自动排版 + 人工微调”的闭环。
图片占位：[图片占位-10-结果编辑器与排版对照-16:9]
图片说明：放入真实大图预览、文本框选中、字体预览或擦除撤销界面。

第 11 页｜任务与批量：如何保证可用性
必须说明任务管理器、单 worker、流水线锁和状态轮询的关系；不能说成多个模型同时推理。
批量流程必须写出：批量校验 → 创建子任务 → 逐张处理 → 汇总平均进度 → 生成 ZIP；保留原文件名和顺序，重名改名，并生成 manifest.json 与 errors.txt。
必须补充安全边界：文件类型和大小校验、批量数量/总大小限制、结果路径只从服务端任务记录解析，避免直接接受客户端任意路径。
视觉要求：上半部分做批量任务时序图，下半部分做“正常/部分成功/失败重试”状态表。
图片占位：[图片占位-11-批量任务界面或下载清单-16:9]
图片说明：放入多图任务进度、部分失败提示或 ZIP 下载界面截图。

第 12 页｜产品界面：从工具到任务工作台
必须展示实际 UI 信息架构：侧边栏中“翻译任务”和“专有名词库”同级；主区域采用接近对话式的任务流；底部支持上传/粘贴并提交；每张图完成后显示预览、重试和下载入口。
必须列出至少四个真实界面细节：任务状态、图片预览、文本编辑、词库管理、局部擦除、批量下载中任选四个。
必须说明设计目的：让用户知道当前任务进行到哪里、哪一张失败、下一步能做什么。
视觉要求：使用 2—4 张真实 UI 截图拼版，截图旁必须有短标签，不能只放一张大图。
图片占位：[图片占位-12-主界面/任务侧边栏/词库/编辑器拼版-16:9]
图片说明：放入实际前端页面截图，统一裁剪比例和浏览器边框。

第 13 页｜AI 协作：人工决策与迭代证据
必须明确分工：人工负责需求约束、优先级、事实核查、运行验证、体验判断和最终取舍；AI 负责代码阅读辅助、问题定位、方案草拟、局部实现和测试建议。
必须展示至少四类真实协作证据：
1. AI 先阅读代码和真实页面，避免凭空编造功能；
2. 针对词库删除问题，人工发现内置词条与用户词条边界，要求删除用户新增词条，AI 完成数据迁移与验证；
3. 针对 AI 润色，人工限定复用已有 DeepSeek、不新增模型、不改变 OCR/翻译/渲染主链，并要求失败保留原译文；
4. 针对擦除、字体预览、文本框拖拽、文件夹上传等功能，人工提出可观察验收条件，AI 实现后由人工运行和复核。
必须单独列出“AI 能力边界”：AI 不能替代真实运行、不能自动证明准确率、不能把规划内容当成已实现、不能绕过人工对 UI 和输出效果的验收。
视觉要求：左侧用“需求 → 分析 → 实现 → 人工验证 → 迭代”流程，右侧预留 2—3 张交互记录截图。
图片占位：[图片占位-13-AI 协作原始记录截图-4:3]
图片说明：放入真实交互记录截图，优先选择有明确约束、问题发现和验证结果的记录。

第 14 页｜怎么证明：测试与现场演示证据
必须展示真实验证结果：截至 2026-09-03，后端 171 项测试通过；前端生产构建成功。测试结果旁标注“验证范围以当前代码和测试环境为准”。
必须规划现场演示顺序：选择日文或英文样例 → 上传/粘贴 → 查看进度 → 展示译图 → 打开编辑 → 修改一处 → 下载结果；如时间不足播放录屏。
必须预留三类证据位：测试命令结果截图、原图/译图对照、实际操作录屏或 UI 截图。
必须主动说明未提供的指标：当前没有经过统一数据集测量的 OCR 准确率、翻译准确率和平均耗时，因此不做虚构比较。
视觉要求：上方显示测试证据，下方用“演示步骤 + 证据截图”组合，不要把测试数字做成夸张的数据大屏。
图片占位：[图片占位-14-测试结果与真实译图对照-16:9]
图片说明：左侧放测试通过截图，右侧放仓库真实日文/英文样例的原译对照。

第 15 页｜结论、边界与答辩入口
必须用三句话总结：已经实现“图片输入—识别—修复—翻译—排版—编辑—下载”的闭环；核心难点在漫画场景下的方向识别、气泡边界、背景修复和可控回退；下一步优先完善历史记录、章节级管理和更多真实样例验证。
必须用两栏列出：当前可验收能力 / 后续可扩展方向。
最后写“谢谢，欢迎提问”，并预留一块小型“现场演示入口”或项目二维码占位，但不要虚构二维码。
图片占位：[图片占位-15-最终译图或项目二维码-1:1]
图片说明：可放一张最有代表性的真实译图；如有项目地址再替换为真实二维码。

【最后检查清单】
生成后请逐页自检：
□ 是否正好 15 页，顺序是否固定；
□ 每页是否都有结论、具体内容和视觉元素；
□ 是否明确区分已实现、依赖环境和后续规划；
□ 是否完整展示 detect → OCR → inpaint → bubble grouping → translate → quality assessment → render；
□ 是否解释批量任务编排但模型推理串行；
□ 是否包含 AI 协作中的人工分工、纠错、决策和能力边界；
□ 是否只使用“171 项测试通过、前端构建成功”等真实验证事实；
□ 是否没有虚构准确率、耗时、加速倍数、GPU、用户数量；
□ 所有图片是否都使用了明确命名的占位框；
□ 是否每页有 60—100 字演讲备注；
□ 是否保证文字可读、对齐统一、元素可编辑。
```

## 二、发送给 Kimi 前需要补充的个人信息

请只修改以下字段，不要改动上面关于项目实现和事实边界的内容：

| 字段 | 填写内容 |
|---|---|
| 汇报人 | 【姓名】 |
| 学号 | 【学号】 |
| 学习方向/班级 | 【学习方向或班级】 |
| 教室号 | 【教室号，如模板要求】 |
| 指导教师 | 【教师姓名】 |
| 汇报日期 | 【日期】 |
| 项目地址/二维码 | 【如有则填写，没有就保留占位】 |

## 三、建议导入的真实素材清单

以下素材优先从项目文件夹和交互记录中挑选，导入 PPT 时按页码替换同名占位框：

| PPT 页码 | 建议素材 | 用途 |
|---|---|---|
| 01 | 项目首页或原图/译图组合 | 封面建立第一印象 |
| 02 | 日文/英文漫画复杂文字局部 | 证明问题真实存在 |
| 04 | 上传、进度、预览界面或录屏截帧 | 证明用户操作闭环 |
| 06 | 流水线图、代码流程或任务日志 | 证明系统处理顺序 |
| 07 | OCR 检测框、竖排识别结果 | 证明识别难点处理 |
| 08 | 原图/掩膜/修复/分组四格图 | 证明图像重建机制 |
| 09 | 词库、翻译结果、质量提示 | 证明翻译可控性 |
| 10 | 大图预览、文本框编辑、擦除撤销 | 证明人工修正能力 |
| 12 | 主界面、任务侧边栏、词库页 | 证明产品化界面 |
| 13 | AI 协作交互记录截图 | 证明人机协作过程 |
| 14 | 171 项测试通过、前端构建、原译对照 | 证明验收依据 |
| 15 | 最终代表性译图或真实项目二维码 | 收束与答辩入口 |

项目中可优先使用的样例：

- 日文样例：`test_image_japanese/e19eca8c467765ba30042abd639840e6.jpg`
- 英文样例：`test_image_english/屏幕截图 2026-08-31 114913.png`
- 中文样例：`test_image_chinese/屏幕截图 2026-08-31 145655.png`
- 操作录屏：`frontend/public/屏幕录制 2026-09-01 143634.mp4`
- AI 协作截图：`docs/正式交付文档/交互记录/截图/`

## 四、现场汇报时的使用建议

这份 PPT 适合“PPT 讲解约 8—10 分钟，现场操作或录屏约 3—4 分钟”。第 06、08、10 页讲技术细节时不要逐字念完所有内容，重点指向图中的输入、处理和输出；第 13、14 页要保留，用于回应老师对 AI 协作和项目真实性的追问。

如果 Kimi 输出的单页过于拥挤，优先缩短句子或扩大页面，不要删除“为什么这样设计”“如何验证”和“已实现/规划边界”这三类信息。
